
/// A class that represents the page of when the user is asked to name the stray cat they found, because the cat they found is not inside the database
class NameCatFoundPage{

  /// A method to build the page
  build(){}
}